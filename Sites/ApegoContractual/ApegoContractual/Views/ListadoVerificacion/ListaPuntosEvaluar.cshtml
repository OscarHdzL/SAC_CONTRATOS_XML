@model Guid
@{
    Layout = "~/Views/MasterPages/Master.cshtml";

    //PMS.ApegoContractual.Negocio.Modelos.SessionOn session = ApegoContractual.GetSessions.GetModelSession();
    //PMS.ApegoContractual.Datos.ContextoApegoContractual.TBLENT_USUARIO usuario = session.Usuario;
    //Guid idcon = new Guid(Session["Contrato"].ToString());

    <!--breadcrumb Dictionary-->
    Dictionary<String, String> ruta = new Dictionary<String, String>();
    ruta.Add(@"\Home\Index", "Inicio");
    ruta.Add(@"\Contrato\Index", "Lista de contratos");
    ruta.Add("-", "Reg. lista de verificación");

    //Guid Dep = PMS.ApegoContractual.Negocio.ContratoNeg.GetDependencia(Model);

}

@section ViewScripts{
    <script src="~/Resources/ListadoVerificacion/Jquery.PuntosAevaluar.js"></script>
    <script src="~/Resources/VistaContrato/Jquery.VistaContrato.js"></script>
}

@*<input type="hidden" id="IdDependencia" value="" />*@
<input type="hidden" id="IdContrato" value="@Model" />
@*<input type="hidden" id="IdUsuario" value="41eb3ef4-363f-11ea-82d7-00155d1b3502" />*@

<!--breadcrumb Partial View-->

@await Html.PartialAsync("~/Views/MasterPages/Breadcrumb.cshtml", ruta)
<input type="text" name="dependenciaContrato" id="dependenciaContrato" hidden />
<section class="content">

    @await Html.PartialAsync("~/Views/VistaContrato/ParcialContrato.cshtml", Model)

    <div class="box">
        <div class="box-header with-border">
            <h3 class="box-title">Verificación de contenido de un contrato</h3>
            <div class="box-tools pull-right">
                <button type="button" class="btn btn-box-tool" data-widget="collapse" data-toggle="tooltip"
                        title="Collapse">
                    <i class="fa fa-minus"></i>
                </button>
            </div>
        </div>
        <div class="box-body">
            <div class="row">
                <div class="col-xs-12">
                    <div class="box box-default">
                        <div class="box-body">
                            <div class="row">
                                <div class="col-lg-12">
                                    <table id="ListadoPuntosEvaluar" class="display nowrap" cellspacing="0" width="100%">
                                        <thead>
                                            <tr>

                                                <th>Pregunta</th>
                                                <th>Personalizado</th>
                                                <th>Asignado</th>
                                                <th>Fecha de verificación</th>
                                                <th>Acciones</th>
                                            </tr>
                                        </thead>
                                    </table>
                                </div>
                            </div>
                            <hr style="color: #808080;" />
                            @*<div class="row">
                                    <div class="col-lg-12" align="center">
                                        <br />
                                        <button class="btn btn-primary" id="GuardarFormulario">Guardar</button>
                                        <button class="btn btn-danger" id="CancelarF">Cerrar</button>
                                    </div>
                                </div>*@
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

